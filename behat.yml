default:
  extensions:
    Behat\MinkExtension:
      base_url: http://localhost:8000
      goutte:
        guzzle_parameters:
          verify: false
  suites:
    default:
      contexts: ['FeatureContext']
